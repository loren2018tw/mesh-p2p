## 1. Metadata 與資料結構擴充

- [x] 1.1 擴充 Rust metadata response schema，加入下載端本地重用判定所需欄位（內容識別、修訂資訊、相容版本）。
- [x] 1.2 新增 metadata 版本相容檢查與錯誤碼映射，確保舊下載頁遇到不相容版本時可得到明確錯誤。
- [x] 1.3 更新前端型別與狀態模型，支援「已下載並分享中」「可重用」等新狀態。

## 2. 分享端種子檔重用機制

- [x] 2.1 定義並實作種子描述檔落地格式與命名規則，儲存在原始分享檔案同目錄。
- [x] 2.2 實作分享啟動前一致性驗證流程（檔案大小/時間戳/內容摘要），一致時重用既有種子描述。
- [x] 2.3 實作驗證失敗時的重建流程，重新計算 piece/hash 並覆蓋更新種子描述檔。
- [x] 2.4 補齊分享端單元測試，覆蓋「重用成功」「重建觸發」「檔案不存在/不可讀」等路徑。

## 3. 下載端 WebTorrent 與本地快取重用

- [x] 3.1 導入 `webtorrent` 前端依賴與封裝層，建立下載頁 WebTorrent client lifecycle。
- [x] 3.2 將目前純 HTTP 下載流程改為 P2P 優先 + HTTP fallback，並保留進度/速度/ETA 呈現。
- [ ] 3.3 後續 change：實作下載頁初始化時的本地檔案掃描與比對流程，已完整檔案標示可重用且不重抓。
- [x] 3.4 實作下載完成後自動加入 seeding，並提供手動停止與頁面卸載回收邏輯。
- [x] 3.5 新增前端整合測試或可重播測試腳本，驗證「第二位下載者從第一位下載者取片段」路徑。

## 4. 可觀測性、UI 與狀態面板

- [x] 4.1 擴充狀態資訊 API，提供協同 seeding 是否生效與 peer 指標欄位。
- [x] 4.2 更新桌面 app 右側資訊面板與下載頁狀態標籤，呈現「已下載並分享中」與錯誤摘要。
- [x] 4.3 補齊日誌與 metrics（HTTP upload、P2P upload、active peers、fallback 次數）。

## 5. 驗證與發佈前檢查

- [x] 5.1 進行多裝置 LAN 測試，驗證分享連結、metadata 初始化、P2P 交換與 HTTP fallback。
- [x] 5.2 驗證停止分享後行為：metadata API 回應 session 失效、新下載初始化被拒絕、既有 seeding 被回收。
- [x] 5.3 更新 README 或操作文件，補充種子重用與下載端協同分享的行為說明與限制。
